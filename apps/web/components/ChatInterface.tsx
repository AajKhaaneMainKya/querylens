'use client'

// TODO: Week 7 — build chat interface
// POST to NEXT_PUBLIC_API_URL/query, render response, display <ChartDisplay />

export default function ChatInterface() {
  return <div>{/* TODO */}</div>
}
